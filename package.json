{
  "name": "@magape-track/monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --filter @magape-track/core build && pnpm --filter @magape-track/docs build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.5"
  }
}
